// Para Fase 1, apenas marcamos como completada a primeira etapa
document.addEventListener("DOMContentLoaded", () => {
  const steps = document.querySelectorAll(".progress-step");
  steps[0].classList.add("completed");
});
